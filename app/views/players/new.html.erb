<h1>Create a new player</h1>

<%= form_for(@player) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :description %>
    <%= f.text_area :description %>

    <%= f.label :rating %>
    <%= f.text_field :rating %>

    <%= f.submit "Create player" %>
<% end %>